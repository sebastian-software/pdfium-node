# @sebastian-software/pdfium-node-linux-x64-gnu

Linux x64 glibc native package for `@sebastian-software/pdfium-node`.

This package contains the Linux x64 glibc native PDFium addon for `@sebastian-software/pdfium-node`.

It currently supports selected-page PNG rendering. JPEG output is still pending.
